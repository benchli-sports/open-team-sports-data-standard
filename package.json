{
  "name": "otsd-validation",
  "version": "0.1.0",
  "private": true,
  "description": "Development tooling that validates OTSD example payloads and export packages against the v0.1 JSON Schemas.",
  "scripts": {
    "test": "node scripts/validate.js",
    "validate": "node scripts/validate.js"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
